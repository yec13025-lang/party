# ---------------------------------------------------------------------------
# 运行时数据
# ---------------------------------------------------------------------------
# data/cards/ 下是真实用户生成的贺卡，内含真人照片、录音与姓名。
# 属于部署方私有数据，任何情况下都不要提交。
/data/cards/

# token 白名单是部署私密配置；仓库只保留加载逻辑，不保留具体值。
/data/tokens.json

# 可选内容源：本机放什么就展示什么，不进仓库以免把本地内容带上去。
# 若确实要随仓库分发示例内容，用 `git add -f data/blog.json` 显式覆盖本规则。
/data/blog.json
/data/changelog.json

# ---------------------------------------------------------------------------
# 依赖 / 构建产物（本项目零依赖，正常不会出现这些目录）
# ---------------------------------------------------------------------------
node_modules/
dist/
build/

# ---------------------------------------------------------------------------
# 环境变量与本地覆盖
# ---------------------------------------------------------------------------
.env
.env.*
*.local

# ---------------------------------------------------------------------------
# 日志与临时文件
# ---------------------------------------------------------------------------
*.log
*.tmp
*.tmp-*
*.bak
*.orig
*.rej
*.swp

# ---------------------------------------------------------------------------
# 操作系统与编辑器垃圾
# ---------------------------------------------------------------------------
.DS_Store
._*
Thumbs.db
desktop.ini
.idea/
.vscode/
*.code-workspace
